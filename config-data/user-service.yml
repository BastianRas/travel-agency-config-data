server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_users_db?useSSL=false&serverTimezone=UTC
    username: root
    password: password # Cambia esto si tu clave de MySQL es distinta
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/